---
import RatgeberArticle from "../../components/RatgeberArticle.astro";
import { fassadendaemmungFoerderungRatgeber } from "../../data/ratgeber/fassadendaemmung-foerderung-nrw.js";

const {
  title,
  description,
  breadcrumbLabel,
  h1,
  intro,
  sections,
  asideTitle,
  asideText,
  asideCtaLabel,
  faqSchema
} = fassadendaemmungFoerderungRatgeber;
---

<RatgeberArticle
  title={title}
  description={description}
  breadcrumbLabel={breadcrumbLabel}
  h1={h1}
  intro={intro}
  sections={sections}
  asideTitle={asideTitle}
  asideText={asideText}
  asideCtaLabel={asideCtaLabel}
  faqSchema={faqSchema}
/>
