---
import RatgeberArticle from "../../components/RatgeberArticle.astro";
import { sanierungKostenDuesseldorfRatgeber } from "../../data/ratgeber/sanierung-kosten-duesseldorf.js";

const {
  title,
  description,
  breadcrumbLabel,
  h1,
  intro,
  sections,
  asideTitle,
  asideText,
  asideCtaLabel,
  faqSchema
} = sanierungKostenDuesseldorfRatgeber;
---

<RatgeberArticle
  title={title}
  description={description}
  breadcrumbLabel={breadcrumbLabel}
  h1={h1}
  intro={intro}
  sections={sections}
  asideTitle={asideTitle}
  asideText={asideText}
  asideCtaLabel={asideCtaLabel}
  faqSchema={faqSchema}
/>
