---
import RatgeberArticle from "../../components/RatgeberArticle.astro";
import { bodenlegerKostenRatgeber } from "../../data/ratgeber/bodenleger-kosten-nrw.js";

const {
  title,
  description,
  breadcrumbLabel,
  h1,
  intro,
  sections,
  asideTitle,
  asideText,
  asideCtaLabel,
  faqSchema
} = bodenlegerKostenRatgeber;
---

<RatgeberArticle
  title={title}
  description={description}
  breadcrumbLabel={breadcrumbLabel}
  h1={h1}
  intro={intro}
  sections={sections}
  asideTitle={asideTitle}
  asideText={asideText}
  asideCtaLabel={asideCtaLabel}
  faqSchema={faqSchema}
/>
